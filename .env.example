# Port for the local server
PORT=4173
# Extra skill folders to scan (colon-separated absolute paths)
SKILLS_UI_EXTRA_DIRS=
# Override the home directory used to locate skill sources (defaults to os.homedir())
SKILLS_UI_HOME=
